language: python
python:
  - "3.9"
# command to install dependencies
install:
  - pip install -r requirements.txt
# command to run tests
before_script: cd app1_sender
script:
  - pytest
  - python3 sender.py